# MyBlog 重建计划书：Quartz 4 → Astro 自建

> 日期：2026-09-23 ｜ 状态：**方案已确认，待实施**（计划开工日 2026-09-24）
> 线上站点：<https://zmdld11.github.io> ｜ 仓库：本仓库（`zmdld11.github.io`，main 分支，GitHub Actions 构建部署）
> 本文档是重建工作的**唯一计划事实源**：实施中方案有变，先改这里再动代码；各 issue 完成后在 §7 表格中回填状态。

---

## 1. 背景与痛点

现有博客基于 Quartz 4.5.2（为 Obsidian 数字花园设计的框架），五个核心痛点：

| # | 痛点 | 原始描述 |
|---|---|---|
| P1 | 界面定制困难 | 调排版/背景/按钮/特效/小组件都要改框架源码 tsx。期望：少动的（按钮样式、字体、布局）进配置文件；常动的（背景、头图、节日版面）有外置接口，能在博客后台直接上传更换 |
| P2 | 发帖流程繁琐 | 只能本地写 md → add/commit/push。期望：像贴吧一样在网站里直接写帖上传，支持 Markdown 和图片插入 |
| P3 | 小组件癖好 | 日历、音乐播放器等一堆奇怪小组件，"可以不用，不能没有" |
| P4 | GitHub 动态 | 要一个小栏目实时同步"我最近在 GitHub 干了什么" |
| P5 | 现代化 | 整个界面和路径形式更现代 |

## 2. 现状诊断（2026-09-23 仓库体检）

- **框架**：Quartz 4.5.2（上游已发 v5.0.0）。整个仓库是 `jackyzha0/quartz` 的完整克隆（1633 commits），自有工作 = main 上叠的 7 个 commit。
- **部署链路正常**：push main → `.github/workflows/deploy.yml` → `npx quartz build` → Pages artifact 上线；`public/` 不入库。
- **内容**：`content/` 下 20 篇 md（8 篇顶层 + 12 篇在 `软件推荐目录/`），图片同名文件夹同目录存放，仓库共 52 MB。
- **Hexo/ShokaX 迁移遗留**：
  - frontmatter 为 Hexo 风格（`title/date/tags/categories/comment/reward`），`comment`/`reward` 是框架不认的死字段；无 `draft` 字段；
  - `content/软件推荐目录.md` 残留 Hexo `{% post_link %}` 语法，不会渲染。
- **配置问题**：
  - `quartz.config.ts` 的 `baseUrl` 仍是上游默认 `quartz.jzhao.xyz` → RSS/sitemap/OG 图的绝对链接全错；
  - 量化看板入口（`quartz/components/ExternalLink.tsx`，侧栏按钮）指向 `http://101.133.134.164:8000`，HTTPS 站点挂 HTTP 链接会被浏览器混合内容策略拦截。
- **其他**：`docs/` 是上游 Quartz 自文档（与本站无关）；`copilot/` 是个人 prompt 收藏（保留不动）；`fix_images.ps1` 为一次性迁移脚本；上游 ci/preview/docker 三套 workflow 在 main 上空转。

**结论**：痛点根源是框架定位（数字花园 ≠ 个人博客），Quartz 的定制模式就是"改框架源码"，换框架比继续改造划算。

## 3. 方案调研

### 3.1 框架选型

| 候选 | 优势 | 劣势 | 结论 |
|---|---|---|---|
| 保留 Quartz 4 深度改造 | 改动最小 | 定制仍要改框架源码，P1/P3/P5 只能部分解决 | 否 |
| Hugo / Hexo + 主题 | 构建快、模板多 | 模板语言死板，小组件要 DIY，同样是"套主题"模式 | 否 |
| Astro + 现成主题二开（Mizuki/fuwari） | 最快见效，自带 bento 组件 | 先接受别人的设计再逐步替换 | 备选（可"抄作业"） |
| **Astro 从零自建** | 岛屿架构、组件即文件、定制自由度最高，内容集合+图片优化完善 | 工作量最大 | **✅ 选定** |

参考主题（不作为基底，作为小组件与视觉的参考）：[Mizuki](https://github.com/matsuzaka-yuki/Mizuki)（fuwari 增强版，bento 风格小组件多）、[fuwari](https://github.com/saicaca/fuwari)。

### 3.2 网页发帖（P2）

- GitHub Pages 是纯静态托管 → 网页发帖的唯一正路：**前端直接调 GitHub Contents API 提交文件到仓库 → 自动触发 Actions 构建**。
- 认证卡点：GitHub OAuth **不支持 PKCE/纯前端换 token**（[官方 issue #40077](https://github.com/decaporg/decap-cms/issues/40077)），必须有一个服务端网关保管 `client_secret`。
- 方案对比：Sveltia CMS（现成，1-2 天）/ Decap CMS（老化）/ **自研发帖页（✅ 选定，体验完全可控、界面与站点一体）**。
- 认证网关也自研：Cloudflare Worker 约 100 行（实现可参考 [sveltia-cms-auth](https://github.com/sveltia/sveltia-cms-auth)），免费层 10 万请求/天，个人使用绰绰有余。

### 3.3 GitHub 动态（P4）

- **动态流**：`GET /users/zmdld11/events/public`，免 token 可用（[官方文档](https://docs.github.com/en/rest/activity/events)）；限额按访客 IP 各自计算（60 次/时），个人站足够。前端 localStorage 缓存 + ETag 条件请求降耗。
- **贡献热力图**：需 GraphQL + token → 在 Actions 构建时用 PAT（Secret 注入）取数生成静态 JSON，token 不进任何前端产物。

### 3.4 参考资料

- Astro 官方文档（islands / content collections / redirects）：<https://docs.astro.build>
- 框架对比与迁移实录：[IONOS: Hugo vs Astro 等](https://www.ionos.com)、[chenhuijing.com 迁移记](https://chenhuijing.com)、[blog.ricardof.dev（Hugo→Jekyll→Quartz→Astro）](https://blog.ricardof.dev)、[emgoto.com 数字花园](https://www.emgoto.com)
- GitHub OAuth 无 PKCE：[decap-cms#40077](https://github.com/decaporg/decap-cms/issues/40077)
- OAuth Worker 参考实现：[sveltia/sveltia-cms-auth](https://github.com/sveltia/sveltia-cms-auth)、[Sveltia CMS](https://sveltiacms.app)

## 4. 决策记录

| # | 决策点 | 结论 | 说明 |
|---|---|---|---|
| D1 | 框架路线 | **Astro 5 从零自建** | 用户拍板 2026-09-23 |
| D2 | 发帖方案 | **自研发帖页 + 自研 CF Worker OAuth 网关** | 用户拍板；"先 CMS 后自研"被否，直接自研 |
| D3 | 首页形态 | **Bento 卡片仪表盘** | 用户拍板 |
| D4 | 首发小组件 | 日历/时钟、音乐播放器、GitHub 动态+贡献图、一言+访客统计（**全选**） | 用户拍板 |
| D5 | 托管 | 继续 GitHub Pages + Actions | 免费稳定，不动（推导） |
| D6 | 仓库策略 | 原地重建；先打 `quartz-archive` 分支保全现状；不改写历史 | 符合红线（推导） |

## 5. 目标架构

### 5.1 技术栈

- Astro 5（纯静态输出）+ TypeScript + Tailwind CSS 4 + Preact islands（小组件/编辑器）
- Node ≥ 20（沿用本机 22，保留 `.node-version`）
- **全资源自托管**，不依赖 Google Fonts / jsDelivr（保证国内可达性）；中文用系统字体栈，可选自托管子集字体

### 5.2 目录结构

```
/
├── astro.config.mjs               # site: https://zmdld11.github.io、redirects 旧路径
├── package.json
├── src/
│   ├── config/
│   │   └── site.ts                # 【少动层】站点信息/字体/按钮与卡片样式令牌/布局参数/小组件开关
│   ├── content.config.ts          # collections 定义：posts / settings
│   ├── content/
│   │   ├── posts/<slug>/          # 文章：index.md + 同目录图片（slug 即目录名）
│   │   └── settings/              # 【常动层】后台可直接改
│   │       ├── theme.json         #   背景/头图/节日版面/主题色覆盖
│   │       ├── notice.json        #   公告栏
│   │       └── playlist.json      #   音乐播放器歌单（支持外链音频）
│   ├── widgets/                   # 小组件 islands，每个一个目录（组件+配置+默认参数）
│   ├── components/                # Astro 布局组件
│   ├── layouts/
│   ├── styles/                    # 设计令牌（CSS 变量）+ 全局样式
│   ├── lib/                       # github api 客户端、oauth、图片压缩、lunar 封装
│   └── pages/
│       ├── index.astro            # Bento 仪表盘首页
│       ├── posts/[...slug].astro
│       ├── tags/[tag].astro
│       ├── archive.astro
│       ├── about.astro
│       └── admin/                 # 自研发帖页 + 设置后台（noindex）
├── public/                        # favicon、media/（后台上传的图）
├── scripts/migrate-quartz.mjs     # 一次性迁移脚本（跑完归档）
├── cloudflare-worker/             # OAuth 网关（独立小项目，wrangler 部署）
└── .github/workflows/deploy.yml   # astro build → Pages + 贡献图取数
```

### 5.3 双层配置接口（P1 核心）

| 层 | 位置 | 内容 | 修改方式 |
|---|---|---|---|
| 少动层 | `src/config/site.ts` | 站点元信息、字体、按钮/卡片样式令牌、布局参数、小组件开关 | 改代码提交（后端调整后上传） |
| 常动层 | `src/content/settings/*.json` + `public/media/` | 背景图、头图、节日版面、公告、歌单 | `/admin/settings` 网页后台改图上传，不碰代码 |

统一生效方式：提交 → Actions 构建 → **约 2 分钟上线**（静态托管固有延迟，已知情接受）。

`theme.json` 字段草案：

```jsonc
{
  "background": { "type": "aurora", "image": "", "blur": 0, "opacity": 0.9 },
  "hero": { "image": "/media/hero/xxx.webp", "title": "", "subtitle": "" },
  "festival": { "enabled": false, "name": "", "effects": ["sakura"] },
  "accentColor": ""   // 留空则用 site.ts 默认
}
```

### 5.4 页面与 URL（P5）

| 路径 | 页面 |
|---|---|
| `/` | Bento 仪表盘首页 |
| `/posts/<slug>/` | 文章页（slug 语义化，拼音/英文） |
| `/tags/<tag>/`、`/archive/`、`/about/` | 标签 / 归档 / 关于 |
| `/admin/`、`/admin/settings/` | 发帖页 / 设置后台（noindex + GitHub 登录校验） |

旧中文路径 → `astro.config.mjs` 的 `redirects` 生成 meta-refresh 跳转页；404 页兜底。

### 5.5 Bento 首页（D3）

```
┌──────────┬────────┬─────────┐
│ 头像/签名 │  日历   │ GitHub  │
│  社交链接 │        │  动态流  │
├─────┬────┴────────┴─────────┤
│音乐 │   最新文章卡片 × 2      │
│播放 ├───────┬───────────────┤
│ 器  │ 一言  │ 天气/访客统计   │
└─────┴───────┴───────────────┘
```

卡片栅格的顺序/跨度/显隐在 `site.ts` 配置。

### 5.6 小组件清单（第一期全做，P3+D4）

| 组件 | 形态 | 技术要点 |
|---|---|---|
| 时钟 | 卡片实时时钟/日期 | Preact island |
| 日历 | 公历 + 农历 + 节日标记 | `lunar-typescript` |
| 音乐播放器 | 悬浮迷你条 + bento 卡片两态 | 原生 `<audio>`；`playlist.json` 歌单；音频建议外链（git 单文件 ≤100MB） |
| GitHub 动态流 | 事件流卡片 | events API + localStorage 缓存 30min + ETag |
| 贡献热力图 | 绿格子 | 构建时 GraphQL → 静态 JSON 渲染 |
| 一言 | 卡片 | hitokoto.cn API |
| 访客统计 | 页脚 | 不蒜子（备选：Worker 自建计数） |

组件注册表：每个 widget 一个目录，`site.ts` 一行开关；关闭后不产生任何网络请求。

### 5.7 自研发帖页（P2 核心）

```
/admin 打开发帖页 → 登录：
  浏览器 → Worker /authorize → GitHub 授权页 → Worker /callback
  （Worker 用环境变量里的 client_secret 换 token，secret 不出网关）
  → token postMessage 回页面，仅存 sessionStorage
  → 校验 GET /user 的 login === "zmdld11"，否则拒绝所有写操作

写帖：Markdown 编辑器 + 实时预览
传图：拖拽/粘贴 → canvas 压缩 → base64 → Contents API
发布：PUT /repos/zmdld11/zmdld11.github.io/contents/src/content/posts/<slug>/...
  （自动生成 slug、日期、frontmatter；新文件无 sha，改文件带 sha）
→ push 触发 Actions → ~2 分钟后上线；页面显示"构建中"状态
```

- 草稿：`draft: true` frontmatter，构建时排除；注：草稿存公开仓库，技术上可见（个人博客接受）。
- 安全：OAuth App scope 最小化 `public_repo`；Worker 校验 origin；token 仅 sessionStorage；`/admin` 加 noindex。

### 5.8 GitHub 动态与贡献图（P4）

见 §3.3；事件流卡片过滤展示 push/issue/PR/star，贡献图用构建期生成的 `src/data/contributions.json` 渲染。

### 5.9 设计系统

- **设计令牌**：明/暗两套 CSS 变量（颜色、圆角、阴影、间距）+ 字体栈，全部集中在 `styles/`；
- **视觉**：玻璃拟态卡片（backdrop-filter）、极光/渐变背景层（theme.json 控制）、卡片微交互、Astro View Transitions 页面过渡；
- **明暗模式**：跟随系统 + 手动切换，localStorage 记忆；
- **排版**：文章页字号/行高/代码块/图片样式统一。

## 6. 迁移方案

### 6.1 仓库策略

```bash
git branch quartz-archive    # 保全现状（含上游历史与自有 7 个 commit），此后不再动
# main 上以普通 commit 删除 Quartz 文件、落入 Astro 骨架，随时可回溯
```

不 force push、不 filter-repo、不改写历史（红线）。

### 6.2 内容迁移（scripts/migrate-quartz.mjs，一次性）

frontmatter 转换表：

| 现状（Hexo 风格） | 新 schema | 备注 |
|---|---|---|
| `title` | `title` | 必填 |
| `date` | `pubDate` | |
| `tags` | `tags` | 兼容数组/单字符串 |
| `categories` | `category` | 可选，单值 |
| `comment` / `reward` | 删除 | 死字段 |
| （新增） | `description` | 摘要，可空 |
| （新增） | `draft` | 默认 false |

- 路径：`content/软件推荐目录/*.md` → `src/content/posts/<slug>/index.md`，同名图片文件夹随迁；
- `{% post_link %}` 残留人工改写为普通链接（数量少）；
- **待定项**：旧文 slug 转拼音（默认）还是保留中文（URL encode），迁移前可改；
- 迁移后 20 篇逐篇人工过一遍渲染效果。

### 6.3 清理与保留

| 处理 | 对象 |
|---|---|
| 删除（archive 分支可找回） | `quartz/`、`docs/`、`Dockerfile`、`fix_images.ps1`、上游 ci/preview/docker workflows、`.nojekyll` |
| 保留 | 20 篇文章（迁移后）、`copilot/`、README（重写） |
| 新增 | Astro 工程、`cloudflare-worker/`、`scripts/` |

### 6.4 部署

改造 `deploy.yml`：checkout → setup Node 22 → `astro build`（前置 step：用 Secret `CONTRIBUTIONS_TOKEN` 拉贡献图生成 JSON）→ upload `dist/` → deploy-pages；保留 `workflow_dispatch` 手动触发。

## 7. 实施计划（issue 化）

按规范每个 issue 写背景/实现规划/验收标准，commit 带 issue 号，完成写小结再关闭。依赖：#1→#2→#3→（#4、#5 可并行）→#6→#7→#8；#6 可与 #4/#5 并行。

| issue | 内容 | 验收标准（摘要） | 状态 |
|---|---|---|---|
| #1 骨架 | `quartz-archive` 分支 + Astro 初始化 + 设计令牌 + `site.ts` 双层接口雏形 + deploy.yml 改造 | 空站 Pages 上线；`npm run dev`/`build` 本地可跑；改一处令牌全站生效 | 待开工 |
| #2 内容迁移 | 迁移脚本 + 20 篇转换 + redirects + 修 baseUrl | 20 篇全可访问、图片不丢、旧路径能跳转、RSS 域名正确 | 待开工 |
| #3 布局与设计系统 | Bento 首页骨架 + 文章/标签/归档/关于 + 明暗模式 + 过渡动画 | 核心页面视觉定稿；本地 Lighthouse 性能 ≥ 90 | 待开工 |
| #4 小组件 | §5.6 清单七件 | `site.ts` 可逐个开关；关闭后无残留网络请求 | 待开工 |
| #5 GitHub 动态 | 事件流 + 贡献图 + PAT secret 配置 | 首页显示真实动态；token 不出现在任何前端产物 | 待开工 |
| #6 OAuth 网关 | `cloudflare-worker/` 部署 + GitHub OAuth App 注册 | 登录往返成功；`client_secret` 仅存 Worker 环境变量 | 待开工 |
| #7 发帖页 + 设置后台 | `/admin` 编辑器 + 传图 + `/admin/settings` | 网页发一篇带图帖 2 分钟内上线；后台换背景图生效 | 待开工 |
| #8 收尾 | RSS/sitemap/SEO、404 页、性能检查、量化入口修正 | RSS 可订阅；404 兜底；量化入口保留并标注 HTTPS 问题 | 待开工 |

## 8. 前置条件（需要用户准备）

1. **Cloudflare 账号**（免费）——issue #6 时给一步步指引；
2. **GitHub OAuth App** 一个（Homepage 填博客地址，Callback 填 Worker 域名）——issue #6；
3. **fine-grained PAT**（仅 `read:user`）→ 配置仓库 Secret `CONTRIBUTIONS_TOKEN` ——issue #5；
4. （可选，不阻塞）量化面板 `101.133.134.164:8000` 上 HTTPS，否则浏览器会继续拦截混合内容。

## 9. 风险与取舍（已知情接受）

| 风险 | 说明 | 对策/态度 |
|---|---|---|
| 变更生效延迟 ~2 分钟 | 静态托管固有 | 接受；发帖页做"构建中"状态提示 |
| 草稿公开可见 | 草稿存公开仓库 | 个人博客接受 |
| 不蒜子第三方不稳 | 统计可能偶尔挂 | 先用；挂了切 Worker 自建 |
| GitHub Pages 国内速度一般 | 现状如此 | 全资源自托管已尽量优化；换托管二期再议 |
| 仓库体积增长 | 已 52MB，后台上传会持续增长 | 前端压缩图片；单文件 ≤100MB 红线；音频走外链 |
| Astro 版本迭代快 | API 可能变化 | 锁死版本号；升级单独开 issue |

## 10. 开工清单（2026-09-24）

1. 把 §7 的 issue #1 建成 GitHub issue（背景/实现规划/验收标准直接引用本文档）；
2. `git branch quartz-archive`（本地操作）；是否 push 该分支到远程听用户指令；
3. main 上初始化 Astro 工程（§5.2 结构），本地 dev 跑通空站；
4. 改造 `deploy.yml`，push 前向用户过目改动，确认后推送并验证 Pages 上线；
5. 继续 #2 迁移脚本。
